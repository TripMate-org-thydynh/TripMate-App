<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget khoảnh khắc squad.

  Bố cục cố tình đơn giản: ảnh chiếm gần hết, chữ nằm trên dải mờ dưới đáy.
  RemoteViews chỉ hỗ trợ một tập widget hạn chế nên không dùng được
  ConstraintLayout hay custom view ở đây.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <ImageView
        android:id="@+id/widget_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/widget_image_desc"
        android:src="@drawable/widget_placeholder" />

    <!-- Dải tối để chữ luôn đọc được dù ảnh sáng hay tối -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="@drawable/widget_scrim"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="16dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/widget_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="@string/widget_empty_title" />

        <TextView
            android:id="@+id/widget_trip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="#E6FFFFFF"
            android:textSize="12sp"
            android:text="@string/widget_empty_body" />
    </LinearLayout>
</FrameLayout>
