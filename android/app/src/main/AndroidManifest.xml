<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Chỉ khai báo quyền app THỰC SỰ dùng. Khai thừa sẽ lệch với bảng
         Data Safety trên Google Play và làm người dùng ngại cài. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CAMERA: Squad Cam (khung ngắm trực tiếp, chụp ảnh và quay clip ngắn),
         image_picker (chụp ảnh vị trí, quét vé). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- RECORD_AUDIO: quay clip ngắn CÓ TIẾNG ở Squad Cam (giữ nút chụp). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- VIBRATE: HapticFeedback ở các thao tác chính. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Máy không có camera vẫn cài được; Squad Cam sẽ báo lỗi rõ ràng. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Đã gỡ (không có code nào dùng):
         ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION — app lấy toạ độ từ EXIF
           ảnh và từ lịch trình, không đọc GPS thiết bị.
         READ_MEDIA_VIDEO — chỉ chọn ảnh (pickImage), clip do app tự quay.
         RECEIVE_BOOT_COMPLETED — không có notification/alarm chạy nền.
         Thêm lại kèm code sử dụng khi làm tính năng tương ứng. -->

    <application
        android:label="TripMate"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Bật deep-linking do Flutter/go_router xử lý (không cần plugin). -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link: link mời chia sẻ https://tripmate.app/join/<code>.
                 android:autoVerify yêu cầu file assetlinks.json trên domain
                 (xem EXTERNAL_SETUP.md). Chưa verify thì Android vẫn hiện
                 hộp chọn app thay vì mở thẳng trình duyệt. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="tripmate.app"
                      android:pathPrefix="/join"/>
            </intent-filter>

            <!-- Custom scheme: hoạt động ngay, không cần verify domain. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tripmate" android:host="join"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Widget màn hình chính: khoảnh khắc mới nhất của squad.
             Dữ liệu do Flutter ghi qua package home_widget. -->
        <receiver
            android:name=".TripMateWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tripmate_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: cho phép canLaunchUrl thấy app thanh toán + trình duyệt -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="momo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="zalopay"/>
        </intent>
    </queries>
</manifest>
